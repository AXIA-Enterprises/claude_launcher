# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
*.egg-info/
build/
dist/

# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp
*.swo

# Build artifacts (icon generation)
AppIcon.icns
AppIcon.iconset/*.png

# Optional .app bundle build outputs
*.app/
ClaudeLauncher.app/

# Local state files (the app writes these to $HOME, but ignore here in case
# anyone runs it from inside the repo and their state ends up alongside)
.claude_launcher_*.json
.claude_launcher_bootstrapped

# Internal audit / process documents — kept local, not part of the public repo
AUDIT/
